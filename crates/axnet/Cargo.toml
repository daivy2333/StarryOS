# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2024"
name = "axnet-ng"
version = "0.3.0-preview.2"
authors = ["Mivik <mivikq@gmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "ArceOS network module"
homepage = "https://github.com/arceos-org/arceos"
readme = false
license = "GPL-3.0-or-later OR Apache-2.0 OR MulanPSL-2.0"
repository = "https://github.com/arceos-org/arceos/tree/main/modules/axnet"

[features]
vsock = ["axdriver/vsock"]
# QEMU-only bounded pressure controls (D9). Propagated exclusively from
# `starry-kernel/qemu`; never enabled by default or in D1/real-board builds.
qemu-diagnostics = []

[lib]
name = "axnet_ng"
path = "src/lib.rs"

[dependencies.async-channel]
version = "2.5"
default-features = false

[dependencies.async-trait]
version = "0.1"

[dependencies.axconfig]
version = "=0.3.0-preview.2"

[dependencies.axdriver]
version = "=0.3.0-preview.2"
features = ["net"]

[dependencies.axdriver_net]
version = "0.1.4-preview.3"

[dependencies.axerrno]
version = "0.2"

[dependencies.axfs-ng]
version = "=0.3.0-preview.2"

[dependencies.axfs-ng-vfs]
version = "0.1"

[dependencies.axhal]
version = "=0.3.0-preview.2"

[dependencies.axio]
version = "0.3.0-pre.1"

[dependencies.axpoll]
version = "0.1"

[dependencies.axsync]
version = "=0.3.0-preview.2"

[dependencies.axtask]
version = "=0.3.0-preview.2"
features = [
    "irq",
    "multitask",
]

[dependencies.bitflags]
version = "2.9"

[dependencies.cfg-if]
version = "1.0"

[dependencies.enum_dispatch]
version = "0.3"

[dependencies.embassy-sync]
version = "0.6.2"
default-features = false

[dependencies.hashbrown]
version = "0.16"

[dependencies.lazy_static]
version = "1.5"
features = ["spin_no_std"]

[dependencies.log]
version = "0.4"

[dependencies.ringbuf]
version = "0.4"
features = ["alloc"]
default-features = false

[dependencies.smoltcp]
path = "../smoltcp"
version = "0.13.1"
features = [
    "alloc",
    "log",
    "async",
    "medium-ethernet",
    "medium-ip",
    "proto-ipv4",
    "proto-ipv6",
    "auto-icmp-echo-reply",
    "socket-raw",
    "socket-icmp",
    "socket-udp",
    "socket-tcp",
    "socket-dns",
]
default-features = false

[dependencies.spin]
version = "0.10"

# Test-only dyn injection for fake NICs; must not leak into product deps.
[dev-dependencies.axdriver]
version = "=0.3.0-preview.2"
features = ["net", "dyn"]

# Exact address types used by the axklib `mem_iomap` trait-ffi ABI witness.
[dev-dependencies.memory_addr]
version = "0.4"

# Host tests need a critical-section implementation for `embassy-sync`'s
# AtomicWaker; the product build relies on the kernel's restore-state-bool impl.
[dev-dependencies.critical-section]
version = "1.2"
features = ["std"]

# Test-only TCP state seed (smoltcp `test-seeds`): the record is outside the
# [dependencies.smoltcp] feature set, so a product `--lib` build never compiles
# the state-forcing API. Dev builds union this edge with the normal dependency;
# default-features must stay false so the test graph adds only `test-seeds`.
[dev-dependencies.smoltcp]
path = "../smoltcp"
version = "0.13.1"
default-features = false
features = ["test-seeds"]

# This crate is excluded from the root workspace, so the root patch table does
# not apply here; redirect axdriver_net to the same workspace-local copy the
# root build uses (queue-control contract lives only there).
[patch.crates-io]
axdriver_net = { path = "../axdriver_net" }
