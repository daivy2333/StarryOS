# CLAUDE.md - StarryOS 项目文档索引（汇总分支）

> Generated by project-rules-generator at 2026-05-24
> Project: StarryOS (feat/uart-async-consolidated)
> Type: Rust OS Kernel (RISC-V)
> 汇总分支：整合 feat/uart-async 和 feat/uart-async-dev2 两个方向的所有经验

---

## 项目概览

StarryOS 是一个基于 RISC-V 的宏内核操作系统，使用 Rust 编写，基于 ArceOS 组件化架构。核心目标是实现异步高性能串口驱动。

**当前状态**：知识整合完成，等待架构层面决策（见 SNAPSHOT.md）

---

## 文档体系

项目文档位于 `.claude/docs/` 目录:

| 文档 | 用途 | 查询方式 |
|------|------|---------|
| SNAPSHOT.md | 项目当前状态快照（汇总） | `grep "关键词" .claude/docs/SNAPSHOT.md` |
| architecture.md | 架构决策记录（ADR-001~023） | `grep "关键词" .claude/docs/architecture.md` |
| tasks.md | 任务追踪（M0~M6 + P0~P6） | `grep "关键词" .claude/docs/tasks.md` |
| learned.md | 项目学习记忆（116+ 条目） | `grep "关键词" .claude/docs/learned.md` |
| references.md | 外部参考与依赖（48+ 条目） | `grep "关键词" .claude/docs/references.md` |
| optimization.md | 优化记录（24 条目） | `grep "关键词" .claude/docs/optimization.md` |
| rules.md | 编码规范（三大规则唯一事实来源） | `grep "关键词" .claude/docs/rules.md` |
| archive.md | 归档内容（已过时的决策和知识） | `grep "关键词" .claude/docs/archive.md` |

---

## 读取顺序

| 场景 | 读取 | 写入 |
|------|------|------|
| 开始新会话 | CLAUDE.md → SNAPSHOT.md → tasks.md | — |
| 写新功能 | rules.md + architecture.md + learned.md | tasks.md, learned.md |
| 修复 Bug | rules.md + SNAPSHOT.md + learned.md | tasks.md, learned.md |
| 重构 | rules.md + architecture.md | architecture.md, learned.md |
| 更新进度 | tasks.md | tasks.md, SNAPSHOT.md |
| 查阅 API | learned.md → references.md | — |
| 记录决策 | architecture.md | architecture.md |
| 记录学习发现 | learned.md | learned.md |
| 记录参考 | references.md | references.md |
| 记录优化点 | optimization.md | optimization.md |

---

## 两个探索方向总结

| 方向 | 分支 | 策略 | 结果 | 关键发现 |
|------|------|------|------|----------|
| **A: 渐进式集成** | feat/uart-async (m0→m1→m2→m3→dev) | 复用 Console，逐步替换 | M0-M2 ✅, M3 ❌ | IRQ 风暴 + TX busy-loop |
| **B: 完全剔除 Console** | feat/uart-async-dev2 | 从零开始，uart_16550 独立初始化 | P0-P1 ✅, P2 ❌ | MMIO 权限问题 |

**关键阻塞**：MMIO 权限问题 — axplat 限制 UART MMIO 权限，内核/ISR 都无法访问

---

## 项目特定约束

### 技术栈

- 语言: Rust (nightly-2026-02-25)
- 目标: RISC-V 64-bit (qemu-riscv64)
- 异步框架: axtask::future + embassy-sync::AtomicWaker
- 构建: Make + Cargo
- 测试: QEMU 运行验证
- 格式化: rustfmt
- 静态分析: clippy

### Git 约束

- 提交信息格式: `feat(uart-async): [描述]` / `fix(uart-async): [描述]` / `refactor(uart-async): [描述]`
- 分支策略: feat/uart-async-consolidated → PR → main
- 禁止把 Claude 列为 co-author

### 关键设计决策

详见 architecture.md（ADR-001 ~ ADR-023），包括：
- 异步运行时选型（ADR-001）
- 中断分发架构（ADR-008）
- 缓冲策略（ADR-004）
- VFS 接口（ADR-003）
- 硬件抽象（ADR-006）
- 渐进式集成失败经验（ADR-016~019）
- 完全剔除 Console 策略（ADR-020~021）
- MMIO 权限问题发现（ADR-022~023）

---

## 检查清单

每次提交前确认（详见 rules.md）：

- [ ] 命名清晰，揭示意图
- [ ] 函数 < 20 行，单一职责
- [ ] 无重复代码
- [ ] 无魔法数字/字符串
- [ ] unsafe 块有 SAFETY 注释
- [ ] 依赖显式注入
- [ ] 核心逻辑有测试覆盖
- [ ] 注释解释"为什么"
- [ ] 已运行 cargo fmt 和 cargo clippy
- [ ] 代码比来时更干净
- [ ] 只改必须改的代码
- [ ] 不添加未要求的功能

---

## Red Flags

```
❌ 假设不明确 → STOP，问
❌ 过度复杂 → 简化
❌ 改动超出请求 → 回滚
❌ unsafe 无 SAFETY 注释 → Iron Law 违规
❌ 顺手添加功能 → Karpathy 违规
❌ Gate BLOCK 不记录 → Workflow 违规
❌ 硬件地址裸写 → 安全违规
❌ 中断处理中有阻塞操作 → 性能违规
```
