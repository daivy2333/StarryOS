# CLAUDE.md - StarryOS 项目文档索引

> Generated by project-rules-generator at 2026-05-24
> Project: StarryOS (feat/uart-async)
> Type: Rust OS Kernel (RISC-V)

---

## 项目概览

StarryOS 是一个基于 RISC-V 的宏内核操作系统，使用 Rust 编写，基于 ArceOS 组件化架构。当前分支 `feat/uart-async` 的核心目标是实现异步高性能串口驱动。

---

## 文档体系

项目文档位于 `.claude/docs/` 目录:

| 文档 | 用途 | 查询方式 |
|------|------|---------|
| rules.md | 编码规范（三大规则唯一事实来源） | `grep "关键词" .claude/docs/rules.md` |
| SNAPSHOT.md | 项目当前状态快照 | `grep "关键词" .claude/docs/SNAPSHOT.md` |
| tasks.md | 任务追踪（进行中/待办/阻塞） | `grep "关键词" .claude/docs/tasks.md` |
| learned.md | 项目学习记忆（API/踩坑/技巧） | `grep "关键词" .claude/docs/learned.md` |
| architecture.md | 架构决策记录（ADR） | `grep "关键词" .claude/docs/architecture.md` |
| references.md | 外部参考与依赖 | `grep "关键词" .claude/docs/references.md` |
| optimization.md | 优化记录 | `grep "关键词" .claude/docs/optimization.md` |

---

## 读取顺序

| 场景 | 读取 | 写入 |
|------|------|------|
| 开始新会话 | CLAUDE.md → SNAPSHOT.md → tasks.md | — |
| 写新功能 | rules.md + architecture.md + learned.md | tasks.md, learned.md |
| 修复 Bug | rules.md + SNAPSHOT.md + learned.md | tasks.md, learned.md |
| 重构 | rules.md + architecture.md | architecture.md, learned.md |
| 更新进度 | tasks.md | tasks.md, SNAPSHOT.md |

---

## 项目特定约束

### 技术栈

- 语言: Rust (nightly-2026-02-25)
- 目标: RISC-V 64-bit (qemu-riscv64)
- 异步框架: axtask::future + embassy-sync::AtomicWaker
- 构建: Make + Cargo
- 测试: QEMU 运行验证
- 格式化: rustfmt
- 静态分析: clippy

### Git 约束

- 提交信息格式: `feat(uart-async): [描述]` / `fix(uart-async): [描述]` / `refactor(uart-async): [描述]`
- 分支策略: feat/uart-async → PR → main
- 禁止把 Claude 列为 co-author

### 关键设计决策

详见 architecture.md（ADR-001 ~ ADR-006），包括异步运行时选型、中断模型、缓冲策略、VFS 接口、硬件抽象等。

### 目录结构（预期新增）

```
kernel/src/
├── drivers/
│   └── uart_async/       # 异步串口驱动
│       ├── mod.rs         # 模块导出
│       ├── driver.rs      # UartAsyncDriver 主结构
│       ├── ring_buffer.rs # 环形缓冲区
│       └── dma.rs         # DMA 传输（P2 阶段）
├── hal/                   # 硬件抽象层（复用/扩展）
└── syscall/               # 系统调用（扩展）
```

---

## 检查清单

每次提交前确认（详见 rules.md）：

- [ ] 命名清晰，揭示意图
- [ ] 函数 < 20 行，单一职责
- [ ] 无重复代码
- [ ] 无魔法数字/字符串
- [ ] unsafe 块有 SAFETY 注释
- [ ] 依赖显式注入
- [ ] 核心逻辑有测试覆盖
- [ ] 注释解释"为什么"
- [ ] 已运行 cargo fmt 和 cargo clippy
- [ ] 代码比来时更干净
- [ ] 只改必须改的代码
- [ ] 不添加未要求的功能

---

## Red Flags

```
❌ 假设不明确 → STOP，问
❌ 过度复杂 → 简化
❌ 改动超出请求 → 回滚
❌ unsafe 无 SAFETY 注释 → Iron Law 违规
❌ 顺手添加功能 → Karpathy 违规
❌ Gate BLOCK 不记录 → Workflow 违规
❌ 硬件地址裸写 → 安全违规
❌ 中断处理中有阻塞操作 → 性能违规
```
